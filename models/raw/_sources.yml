version: 2

sources:
  - name: raw          # The database/schema name (logical name for the source)
    schema: csv    # The schema where the table is located
    database: RAW
    tables:
      - name: medical_data  # The table name